<Page x:Class="P2Project.View.VideoPlayerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:P2Project.View"
      mc:Ignorable="d" 
      Title="VideoPlayerPage" MinHeight="130" MinWidth="180">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <MediaElement Grid.Row="0" Name="VideoPlayer" Source="{Binding VideoPath}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
                <Button Grid.Column="0"/>
                <Slider Grid.Column="1" VerticalAlignment="Center" />
                <Button Grid.Column="2"/>
                <Button Grid.Column="3" Command="{Binding FullScreenCommand}" />
        </Grid>
            
    </Grid>
</Page>
